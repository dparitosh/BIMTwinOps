# =============================================================================
# SMART_BIM Frontend Configuration
# =============================================================================
# Copy this file to .env and modify as needed.
# Vite env vars must start with VITE_ to be exposed to the browser.
# =============================================================================

# Backend FastAPI URL
# Must match the backend host/port. Restart the Vite dev server after changing VITE_* values.
VITE_BACKEND_API_URL=http://127.0.0.1:8000

# APS Service URL
VITE_APS_API_URL=http://127.0.0.1:3001
